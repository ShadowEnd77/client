import React, { FC, } from 'react'
import { HasChildren } from '../../../types/common/utilitarian.types'

export const CenteredLayout: FC<HasChildren> = ({ children }) => {
    return (
        <div>
            
        </div>
    )
}
